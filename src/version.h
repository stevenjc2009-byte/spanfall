#ifndef SF_VERSION_H
#define SF_VERSION_H

/* Single source of truth for the app version. CMakeLists.txt parses this line to derive
 * VITA_VERSION (param.sfo APP_VER); see the mapping note there. */
#define SF_VERSION "1.0.0"

#endif
